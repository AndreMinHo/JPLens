# Railway Environment Variables for JPLens Master App

# ==============================================================================
# OPTION 1: Internal URLs with Reference Variables (RECOMMENDED)
# ==============================================================================
# This is the best approach as it automatically uses Railway's private network
# Replace 'jplenscontext' and 'jplensaicontext' with your actual service names
JPLENS_CONTEXT_URL=http://${{jplenscontext.RAILWAY_PRIVATE_DOMAIN}}:${{jplenscontext.PORT}}
JPLENS_AI_CONTEXT_URL=http://${{jplensaicontext.RAILWAY_PRIVATE_DOMAIN}}:${{jplensaicontext.PORT}}

# NOTE: You must manually set a PORT service variable on each backend service
# that matches the port they listen on (Railway doesn't auto-resolve this)

# ==============================================================================
# OPTION 2: Direct Internal URLs
# ==============================================================================
# Use the .railway.internal domain with service name and port
# JPLENS_CONTEXT_URL=http://jplenscontext.railway.internal:8000
# JPLENS_AI_CONTEXT_URL=http://jplensaicontext.railway.internal:8001

# ==============================================================================
# OPTION 3: Public URLs (Alternative)
# ==============================================================================
# Use only if services are in different projects or internal networking has issues
# JPLENS_CONTEXT_URL=https://jplenscontext-production.up.railway.app
# JPLENS_AI_CONTEXT_URL=https://jplensaicontext-production.up.railway.app

# ==============================================================================
# IMPORTANT NOTES:
# ==============================================================================
# ✓ Internal URLs use HTTP (not HTTPS)
# ✓ Service name must match Railway service name (check Settings tab)
# ✓ Port must match what your backend service listens on (check logs)
# ✓ All services must be in the same Railway project for internal networking
# ✓ This app listens on :: (all interfaces) for IPv4/IPv6 support
# ✓ Format: http://servicename.railway.internal:PORT

# ==============================================================================
# OTHER VARIABLES
# ==============================================================================
# Port (automatically set by Railway, no need to set manually)
PORT=3000

# Optional password authentication (only enable if you want to protect the app)
# APP_PASSWORD=your_secure_password_here
